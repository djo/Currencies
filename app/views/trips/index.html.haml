%strong Visited countries:
= @summary[:visited_countries]
%br
%strong Dates:
= trip_dates(@summary[:dates])

#trips
  - @trips.each do |trip|
    .trip
      %h4= countries trip
      = trip.description
      .date=l trip.completed_at, :format => :long

- if user_signed_in?
  = link_to "New Trip", new_trip_path
